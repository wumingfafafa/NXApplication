<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!--
         <style name="drawableationUpPopup">
        <item name="android:windowEnterdrawableation">@drawable/grow_fade_in_from_bottom</item>
        <item name="android:windowExitdrawableation">@drawable/shrink_fade_out_from_bottom</item>
    </style>

    <style name="drawableationFromButtom" >
        <item name="android:windowEnterdrawableation">@drawable/popup_form_bottom</item>
        <item name="android:windowExitdrawableation">@drawable/drop_down_to_bottom</item>
    </style>

    <style name="drawableationFromTop" >
        <item name="android:windowEnterdrawableation">@drawable/drop_down_from_top</item>
        <item name="android:windowExitdrawableation">@drawable/hide_to_top</item>
    </style>
    -->
    <style name="my_text_style">
        <item name="android:textColor">@color/res_color_black</item>
        <item name="android:textSize">@dimen/font_size_28px</item>
        <item name="android:gravity">center_vertical</item>
    </style>

    <style name="MyRatingBar" parent="@android:style/Widget.RatingBar">
        <item name="android:progressDrawable">@drawable/myratingbar</item>
        <item name="android:minHeight">15dip</item>
        <item name="android:maxHeight">15dip</item>
    </style>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

</resources>